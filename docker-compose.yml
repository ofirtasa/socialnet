services:
  app:
    build:
      context: .
      target: runtime
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped

  seed:
    build:
      context: .
      target: seed
    env_file:
      - .env
    profiles:
      - tools
